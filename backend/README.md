# Go Backend

A small Go server for the NFT homepage.

## Run

```powershell
cd backend
go run main.go
```

## API

- `http://localhost:8080/api/status`
- `http://localhost:8080/api/hello`

## Notes

If `go` is not found, restart your terminal or add `C:\Program Files\Go\bin` to your PATH.
